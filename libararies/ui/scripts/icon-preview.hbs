<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>{{fontName}}</title>
  <style>
    body {
      font-family: sans-serif;
      margin: 0;
      padding: 10px 20px;
    }

    .preview {
      line-height: 2em;
    }

    .preview__icon {
      display: inline-block;
      width: 32px;
      text-align: center;
    }

    .MaterialIcon {
      display: inline-block;
      font-size: 24px;
    }
      {{{styles}}}
  </style>
</head>
<body>
<h1>{{fontName}}</h1>
{{#each names}}
  <div class="preview">
    <span class="preview__icon">
      <i class="MaterialIcon {{../classPrefix}}{{this}}"></i>
    </span>
    <span>{{this}}</span>
  </div>
{{/each}}
</body>
</html>
